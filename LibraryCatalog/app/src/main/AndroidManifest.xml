<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="raienryu97.librarycatalog">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AboutTeam" android:theme="@style/AppTheme.NoActionBar">  </activity>
        <activity android:name=".AllBooks" android:theme="@style/AppTheme" >  </activity>
        <activity android:name=".CSc" android:theme="@style/AppTheme" >  </activity>
        <activity android:name=".Ele" android:theme="@style/AppTheme" >  </activity>
        <activity android:name=".Math" android:theme="@style/AppTheme" >  </activity>
        <activity android:name=".Mech" android:theme="@style/AppTheme" >  </activity>
        <activity android:name=".Phy" android:theme="@style/AppTheme" >  </activity>
    </application>

</manifest>